# Earshot — Frontend

React + Vite single-page app for Earshot. See the [root README](../README.md) for the full project overview, architecture, and setup.

```bash
npm install
cp .env.example .env   # optional — sets VITE_API_URL
npm run dev            # http://localhost:5173
```
